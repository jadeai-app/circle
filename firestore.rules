rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all reads and writes.
    // This is a secure default. All access must be explicitly granted.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}